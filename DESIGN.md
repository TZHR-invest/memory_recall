# memory_recall 设计文档

**版本**：v0.1  
**创建时间**：2026-03-19 00:21  
**状态**：草稿

---

## 1. 项目定位

### 1.1 核心目标

**优先级排序**：
1. **首先解决人类记忆痛点**：
   - 想知道某个时间段发生过什么
   - 想起一件事但不记得全貌
   - 找东西忘记在哪
   - 看照片忘记在哪拍的
   - 项目搁置不知从何下手
   - 年纪大健忘
   - 灵感一闪而过忘记

2. **其次服务 AI Agent**：
   - 解决上下文窗口有限问题
   - 解决模型注意力分散问题
   - 提供高质量按需召回，提高 AI Agent 产出质量

### 1.2 问题定义

**人类记忆痛点**：
- 时间追溯：想知道某时间段发生了什么
- 事件召回：想起一件事但不记得全貌
- 位置记忆：找东西忘记在哪
- 照片回忆：看照片忘记在哪拍的
- 项目延续：项目搁置不知从何下手
- 健忘问题：年纪大健忘
- 灵感捕捉：灵感一闪而过忘记

**AI Agent 痛点**：
- 上下文窗口有限
- 模型注意力分散
- 记忆写入不稳定
- 记忆召回被动

---

## 2. 现有系统分析

### 2.1 OpenClaw 内置记忆系统

**优势**：
| 特性 | 说明 |
|------|------|
| 简单可靠 | Markdown 文件存储，易于理解和维护 |
| 语义搜索 | 向量索引，支持语义相似度搜索 |
| 混合搜索 | BM25 + 向量混合检索 |
| 多 provider | 支持 OpenAI、Bailian 等多种 embedding provider |

**局限**：
| 问题 | 说明 |
|------|------|
| 写入不稳定 | Memory Flush 触发时机不可控，依赖模型主动性 |
| 读取浪费 | 每次启动全量读入 MEMORY.md，占用上下文 |
| 召回被动 | 完全依赖模型意识到要搜索，没有主动推送 |
| 维护成本高 | 需手动精简 MEMORY.md，手动提醒模型记录 |

**核心缺陷**：
1. **记录写入不稳定**：Memory Flush 需达到阈值才触发，依赖模型判断
2. **全量注入浪费**：每次启动全量读入，占用大量 token
3. **向量查询触发不明确**：完全依赖模型意识到要搜索

### 2.2 设计启示

**memory_recall 需要解决的问题**：
1. **按需召回**而非全量注入
2. **主动召回机制**而非被动等待
3. **降低维护成本**而非手动精简

---

## 3. 数据模型设计

### 3.1 半结构化模型

**设计原则**：半结构化 + 大模型自动填写

```json
{
  "id": "uuid",
  "content": "今天在咖啡店遇到老同学，聊了很久",
  "created_at": "2026-03-19T00:10:00Z",
  "time": {
    "value": "2026-03-19T14:30:00Z",
    "source": "inferred",
    "confidence": 0.8
  },
  "location": {
    "name": "咖啡店",
    "need_confirm": true
  },
  "people": [
    {
      "name": "老同学",
      "need_confirm": true
    }
  ],
  "emotion": {
    "value": "开心",
    "confidence": 0.7
  },
  "tags": ["社交", "友谊", "聊天"],
  "duration": {
    "value": "2小时",
    "source": "inferred"
  },
  "attachments": []
}
```

### 3.2 字段分类

| 类型 | 字段 | 缺失时处理 |
|------|------|-----------|
| **关键字段** | time, location, people | 缺失时智能判断是否询问 |
| **可选字段** | emotion, tags, duration | 大模型推断，不询问 |

**关键字段处理逻辑**：
1. **time**：优先从内容提取，缺失则询问
2. **location**：优先从内容提取，模糊则标记 `need_confirm`
3. **people**：优先从内容提取，未知人物标记 `need_confirm`

**可选字段处理逻辑**：
1. **emotion**：大模型推断，不询问
2. **tags**：大模型推断，不询问
3. **duration**：大模型推断，不询问

### 3.3 智能询问机制

**询问判断规则**：

| 场景 | 是否询问 | 示例 |
|------|---------|------|
| 关键字段时间模糊 | 询问 | "昨天" → 问具体时间 |
| 关键字段位置模糊 | 标记确认 | "某咖啡店" → `need_confirm: true` |
| 关键字段人物未知 | 标记确认 | "老同学" → `need_confirm: true` |
| 可选字段缺失 | 不询问 | 情绪、标签推断 |

---

## 4. 多模态输入处理

### 4.1 设计原则

**数据模型统一，处理流程分层**：

```
不同输入（文本/语音/图片/视频）
    ↓
不同的预处理流程
    ↓
统一的结构化记忆
    ↓
统一的存储和召回
```

### 4.2 处理优先级

| 优先级 | 输入类型 | 预处理流程 |
|--------|---------|-----------|
| P0 | 文本 | 直接大模型提取 |
| P0 | 图片 | OCR/图像识别 → 大模型提取 |
| P1 | 语音 | ASR 转文本 → 大模型提取 |
| P2 | 视频 | 关键帧提取 → 大模型提取 |

### 4.3 图片处理流程

```
图片输入
    ↓
OCR 提取文字
    ↓
图像识别（场景、物体、人脸）
    ↓
大模型提取结构化信息
    ↓
统一记忆存储
```

**示例**：
```
输入：咖啡店照片
OCR：无文字
图像识别：室内场景、咖啡杯、窗户
大模型提取：
{
  "location": { "name": "咖啡店", "need_confirm": true },
  "time": { "source": "metadata" },
  "tags": ["咖啡", "室内", "休闲"]
}
```

---

## 5. 存储方案设计

### 5.1 存储选型

**候选方案**：

| 方案 | 优势 | 劣势 | 适用性 |
|------|------|------|--------|
| SQLite | 轻量、无依赖 | 不支持向量搜索 | ❌ 不适合 |
| PostgreSQL + pgvector | 关系型 + 向量 | 部署复杂 | ✅ 适合生产 |
| Qdrant | 专业向量数据库 | 部署复杂 | ✅ 适合大规模 |
| 本地 JSONL + 向量索引 | 简单、可移植 | 性能有限 | ✅ 适合 MVP |

**MVP 阶段选择**：本地 JSONL + 向量索引

### 5.2 存储结构

**文件结构**：
```
memory_recall/
├── memories.jsonl          # 所有记忆记录
├── index/
│   ├── time_index.json     # 时间索引
│   ├── location_index.json # 位置索引
│   ├── people_index.json   # 人物索引
│   └── tags_index.json     # 标签索引
└── embeddings/
    └── embeddings.npy      # 向量索引
```

### 5.3 索引策略

**多维度索引**：

| 索引类型 | 用途 | 更新频率 |
|---------|------|---------|
| 时间索引 | 时间范围查询 | 每次写入更新 |
| 位置索引 | 位置关键词查询 | 每次写入更新 |
| 人物索引 | 人物关联查询 | 每次写入更新 |
| 标签索引 | 标签分类查询 | 每次写入更新 |
| 向量索引 | 语义相似度搜索 | 批量更新 |

---

## 6. 召回机制设计

### 6.1 触发方式

**召回触发类型**：

| 类型 | 触发条件 | 示例 |
|------|---------|------|
| **主动召回** | 基于上下文关键词 | 提到"咖啡店"时召回相关记忆 |
| **时间召回** | 时间范围查询 | "上周发生了什么" |
| **关键词召回** | 关键词匹配 | "和老同学相关的记忆" |
| **语义召回** | 向量相似度 | "最近开心的事" |

### 6.2 召回流程

```
召回请求
    ↓
关键词提取
    ↓
多索引查询
    ↓
结果融合
    ↓
相关性排序
    ↓
返回 Top-K
```

### 6.3 排序算法

**多因素排序**：

```
score = w1 * 时间相关性 
      + w2 * 关键词匹配度 
      + w3 * 语义相似度
      + w4 * 访问频率
```

**权重配置**：

| 因素 | 权重 | 说明 |
|------|------|------|
| 时间相关性 | 0.3 | 近期记忆优先 |
| 关键词匹配度 | 0.3 | 精确匹配优先 |
| 语义相似度 | 0.3 | 语义相关优先 |
| 访问频率 | 0.1 | 常访问优先 |

---

## 7. 大模型提取 Prompt 设计

### 7.1 关键字段识别规则

**字段状态判断标准**：

| 状态 | 定义 | 示例 | 处理方式 |
|------|------|------|---------|
| **完整** | 明确、具体、可直接使用 | "2026-03-19 下午3点在星巴克（三里屯店）" | 直接提取，`need_confirm: false` |
| **模糊** | 有信息但不具体 | "昨天下午在某个咖啡店" | 提取 + 标记 `need_confirm: true` |
| **缺失** | 完全没有相关信息 | "心情不错" | 智能判断是否询问 |
| **推断** | 从上下文推断 | "终于下班了" → 推断时间 | 标记 `source: "inferred"` |

**关键字段判断流程**：

```
输入文本
    ↓
time 判断：
├─ 明确时间词（"今天"、"2026-03-19"）→ 完整
├─ 相对时间词（"昨天"、"上周"）→ 模糊，标记 need_confirm
├─ 时间推断词（"下班后"、"午饭时"）→ 推断
└─ 无时间信息 → 缺失
    ↓
location 判断：
├─ 明确地点名（"星巴克三里屯店"）→ 完整
├─ 泛指地点（"咖啡店"、"公司"）→ 模糊，标记 need_confirm
├─ 推断地点（"看电影" → 影院）→ 推断
└─ 无地点信息 → 缺失
    ↓
people 判断：
├─ 明确人名（"张三"、"李四"）→ 完整
├─ 身份指代（"老板"、"同事"）→ 模糊，标记 need_confirm
├─ 关系指代（"老同学"、"朋友"）→ 模糊，标记 need_confirm
└─ 无人物信息 → 缺失
```

### 7.2 智能询问机制

**询问决策矩阵**：

| 场景 | time 缺失 | location 缺失 | people 缺失 | 是否询问 | 询问内容 |
|------|----------|--------------|------------|---------|---------|
| 纯心情记录 | ✅ | ✅ | ✅ | **否** | 情绪记忆不需要时空信息 |
| 事件记录 | ❓ | ❓ | ❓ | **是** | 询问关键字段 |
| 位置相关 | ✅ | ❌ | ✅ | **否** | 位置信息已足够 |
| 人物相关 | ✅ | ✅ | ❌ | **否** | 人物信息已足够 |

**询问优先级**：
1. **time** > **location** > **people**（时间最重要）
2. **事件记录** > **心情记录**（事件需要时空信息）
3. **首次询问** > **后续询问**（避免反复追问）

**询问频率控制**：
- 单次输入最多询问 **2 个字段**
- 询问内容简洁明确
- 提供候选选项（如有历史数据）

**询问示例**：

```
用户输入："今天遇到了一个有趣的人"

提取结果：
{
  "time": { "value": "2026-03-19", "source": "extracted" },
  "location": { "name": null, "need_confirm": false },
  "people": [{ "name": "有趣的人", "need_confirm": true }],
  "need_questions": [
    "这个有趣的人是谁？"
  ]
}

系统回复：
"记录成功！这个有趣的人是谁呢？"
```

### 7.3 文本提取 Prompt（优化版）

```markdown
你是一个记忆提取助手。请从用户输入中提取结构化记忆。

## 输入
{用户输入}

## 当前时间
{当前时间 ISO 8601}

## 提取规则

### time 字段
1. **明确时间**：直接提取，转换为 ISO 8601 格式
   - "今天下午3点" → 2026-03-19T15:00:00
   - "昨天晚上" → 2026-03-18T20:00:00（推断晚上）
2. **相对时间**：基于当前时间计算
   - "上周一" → 计算具体日期
   - "三天前" → 计算具体日期
3. **推断时间**：从上下文推断
   - "下班后" → 推断为工作时间后
   - "午饭时" → 推断为 12:00-13:00
4. **缺失时间**：如果完全不涉及时间，设为 null

### location 字段
1. **明确地点**：直接提取
   - "星巴克三里屯店" → { "name": "星巴克三里屯店", "need_confirm": false }
2. **泛指地点**：提取 + 标记确认
   - "咖啡店" → { "name": "咖啡店", "need_confirm": true }
3. **推断地点**：从上下文推断
   - "看电影" → { "name": "影院", "source": "inferred", "need_confirm": true }
4. **缺失地点**：如果不涉及地点，设为 null

### people 字段
1. **明确人名**：直接提取
   - "和张三、李四" → [{ "name": "张三" }, { "name": "李四" }]
2. **身份指代**：提取 + 标记确认
   - "和老板" → [{ "name": "老板", "need_confirm": true, "relation": "上级" }]
3. **关系指代**：提取 + 标记确认
   - "遇到老同学" → [{ "name": "老同学", "need_confirm": true, "relation": "同学" }]
4. **缺失人物**：如果不涉及人物，设为 null

### emotion 字段（可选，不询问）
从文本推断情绪，置信度 0-1：
- "开心"、"高兴" → { "value": "开心", "confidence": 0.9 }
- "有点烦" → { "value": "烦躁", "confidence": 0.7 }
- 无明显情绪 → { "value": "平静", "confidence": 0.5 }

### tags 字段（可选，不询问）
生成 3-5 个标签，优先级：
1. 事件类型（社交、工作、休闲）
2. 情绪类型（开心、焦虑、平静）
3. 地点类型（咖啡店、办公室、家）
4. 人物关系（朋友、同事、家人）

## 智能询问判断

### 需要询问的场景
1. **事件记录** + 时间缺失 → 询问时间
2. **事件记录** + 地点缺失 → 询问地点（可选）
3. 人物未知 + "need_confirm: true" → 询问人物身份

### 不需要询问的场景
1. **心情记录** + 时空缺失 → 不询问
2. **位置记忆** + 地点明确 → 不询问其他
3. 单字段已足够 → 不询问其他

### 询问格式
```json
{
  "need_questions": ["问题1", "问题2"]
}
```

## 输出格式
```json
{
  "time": {
    "value": "ISO 8601 或 null",
    "source": "extracted/inferred/null",
    "confidence": 0.0-1.0
  },
  "location": {
    "name": "地点名称 或 null",
    "need_confirm": true/false,
    "source": "extracted/inferred/null"
  },
  "people": [
    {
      "name": "人物名称",
      "need_confirm": true/false,
      "relation": "关系（如知道）"
    }
  ],
  "emotion": {
    "value": "情绪",
    "confidence": 0.0-1.0
  },
  "tags": ["标签1", "标签2", "标签3"],
  "need_questions": ["需要询问的问题（最多2个）"]
}
```

## 示例

### 示例 1：完整信息
输入："今天下午3点在星巴克遇到了老同学张三，聊得很开心"
输出：
```json
{
  "time": { "value": "2026-03-19T15:00:00", "source": "extracted", "confidence": 0.9 },
  "location": { "name": "星巴克", "need_confirm": false, "source": "extracted" },
  "people": [
    { "name": "张三", "need_confirm": false, "relation": "同学" }
  ],
  "emotion": { "value": "开心", "confidence": 0.9 },
  "tags": ["社交", "同学", "咖啡店", "开心"],
  "need_questions": []
}
```

### 示例 2：信息模糊
输入："昨天遇到一个有趣的人"
输出：
```json
{
  "time": { "value": "2026-03-18T12:00:00", "source": "inferred", "confidence": 0.6 },
  "location": { "name": null, "need_confirm": false, "source": "null" },
  "people": [
    { "name": "有趣的人", "need_confirm": true, "relation": "未知" }
  ],
  "emotion": { "value": "好奇", "confidence": 0.7 },
  "tags": ["社交", "陌生人", "好奇"],
  "need_questions": ["这个有趣的人是谁？"]
}
```

### 示例 3：心情记录
输入："今天心情不错"
输出：
```json
{
  "time": { "value": "2026-03-19", "source": "extracted", "confidence": 0.9 },
  "location": { "name": null, "need_confirm": false, "source": "null" },
  "people": [],
  "emotion": { "value": "开心", "confidence": 0.8 },
  "tags": ["心情", "开心"],
  "need_questions": []
}
```
```

### 7.4 图片提取 Prompt

```markdown
你是一个记忆提取助手。请从图片中提取结构化记忆。

## 图片信息
- OCR 结果：{OCR 文字}
- 场景识别：{场景}
- 物体识别：{物体列表}
- 拍摄时间：{图片 metadata 中的时间}

## 提取规则
1. **location**：从场景和物体推断位置
2. **time**：从图片 metadata 获取
3. **tags**：从物体和场景生成

## 输出格式
同文本提取
```

### 7.5 提取准确率优化策略

**策略 1：Few-shot 示例**
- 在 prompt 中提供 3-5 个高质量示例
- 覆盖常见场景（完整信息、模糊信息、心情记录）

**策略 2：分步提取**
```
第一步：提取 time、location、people
第二步：推断 emotion 和 tags
第三步：判断是否需要询问
```

**策略 3：置信度评分**
- 每个字段输出置信度
- 低置信度字段标记 `need_confirm`
- 用于后续人工验证

**策略 4：用户反馈学习**
- 记录用户修正历史
- 优化提取规则
- 个性化询问策略

---

## 8. 技术栈

### 8.1 MVP 阶段

| 组件 | 技术选择 | 原因 |
|------|---------|------|
| 存储 | JSONL 文件 | 简单、可移植 |
| 向量索引 | Faiss / numpy | 轻量级 |
| 大模型 | OpenAI / 本地模型 | 提取结构化信息 |
| OCR | Tesseract / PaddleOCR | 图片文字识别 |
| ASR | Whisper | 语音转文字 |

### 8.2 生产阶段

| 组件 | 技术选择 | 原因 |
|------|---------|------|
| 存储 | PostgreSQL + pgvector | 关系型 + 向量 |
| 向量索引 | Qdrant | 专业向量数据库 |
| 大模型 | OpenAI API | 稳定可靠 |
| OCR | 百度 OCR API | 中文识别准确 |
| ASR | 阿里 ASR API | 中文识别准确 |

---

## 9. 里程碑规划

### Phase 1: MVP（1-2 周）

**目标**：验证核心流程

**功能**：
- [ ] 文本输入 → 结构化记忆
- [ ] 时间/关键词/标签索引
- [ ] 基础召回功能

**产出**：
- 核心代码
- 测试用例
- 基础文档

### Phase 2: 多模态（2-3 周）

**目标**：支持图片输入

**功能**：
- [ ] 图片 OCR 识别
- [ ] 图片场景识别
- [ ] 图片记忆存储

**产出**：
- 多模态处理模块
- 图片记忆测试

### Phase 3: 生产化（3-4 周）

**目标**：生产环境部署

**功能**：
- [ ] PostgreSQL 存储
- [ ] 向量数据库集成
- [ ] Web 界面
- [ ] API 服务

**产出**：
- 生产环境部署
- 用户文档
- API 文档

---

## 10. 风险与挑战

### 10.1 技术风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 大模型提取准确率 | 中 | 人工验证 + 用户反馈 |
| 向量索引性能 | 低 | 批量更新 + 缓存 |
| 多模态处理复杂度 | 中 | 分阶段实现 |

### 10.2 产品风险

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| 用户接受度 | 高 | MVP 验证 + 用户访谈 |
| 隐私安全 | 高 | 本地存储 + 加密 |
| 维护成本 | 中 | 自动化维护 |

---

## 11. 下一步行动

- [ ] 实现文本输入 → 结构化记忆核心模块
- [ ] 实现时间/关键词索引
- [ ] 实现基础召回功能
- [ ] 编写测试用例
- [ ] 创建 Web 界面原型

---

*创建时间：2026-03-19 00:21*  
*创建者：颓弟*
