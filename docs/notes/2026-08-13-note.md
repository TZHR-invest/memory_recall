# 2026-08-13 工作记录

## 外部调研流程优化（方法论固化）

基于本次压缩 hook 外部调研复盘，固化三阶段漏斗到 [AGENTS.md](../../AGENTS.md) 与
[notes/README.md](README.md#外部调研模板配合-agentsmd-外部调研节)：

- 定向轮（每平台 ≤3 题，按平台打包）→ 追问轮（只追冲突）→ 收敛轮（回源码）；
- 问题分类 S（源码可验证）/ C（信息差）/ D（决策无关），D 直接砍；
- 平台画像：ChatGPT/Grok 源码级可靠；Claude 诚实但可能漏检；Gemini/doubao 字段易编造；
- 停止条件：源码 + ≥2 平台一致即收敛。

## 外部调研自动化方向

- **API 自动化不接受**：会丢失各平台网页端的数据差异性（社区检索、登录态、账号数据源）；
- 探索**浏览器自动化**：自定义 Chrome 扩展 + 本地桥（127.0.0.1 WebSocket/native messaging）
  + Agent 侧 runner，操作用户已登录的 Chrome 标签页；
- 风险：DOM 变更、平台 ToS/风控、桥安全（白名单 + 本地 token + 人工确认模式）；
- 计划：P1 单平台原型（ChatGPT）→ P2 多平台适配 → P3 安全打磨（未开始）。

## 结论 / 下一步

- 方法论已固化，后续外部调研按新流程执行；
- 浏览器自动化待用户确认是否开始 P1 原型。

## AGENTS.md 精炼重构（内容拆分）

按"AGENTS.md 只留每次请求必带的约束 + 指针"原则拆分：

- 架构/模块地图（含惰性导入模式、死代码清单、scripts 说明）→ `docs/ARCHITECTURE.md`；
- 测试（三层分级、环境注意点、常用命令）→ `docs/TESTING.md`；
- 客户端插件 → `docs/PLUGINS.md`；
- 外部调研工作流 → `docs/RESEARCH_GUIDE.md`（原文整体迁移，notes/README.md 模板引用同步更新）；
- 安装/部署本就在 `docs/DEPLOYMENT.md`，AGENTS.md 原 Setup 节删除；
- AGENTS.md 保留：项目一句话 + 关键约束（运行目录 / 无迁移框架 / VOLC_API_KEY / Auth / 循环导入 /
  两种取代语义 / 死代码）+ 快速启动 + 文档索引表 + 强制文档规范。

关联：`docs/README.md` 索引已更新；`docs/DOCUMENTATION_GUIDE.md` 的 AGENTS.md 锚点链接保持有效。

## project-codex / project-.codex 双容器来源解密（用户提问排查）

背景：用户发现 memories 表同时存在 {keyId}_project-codex 与 {keyId}_project-.codex
两个容器，问来源。排查结论（全部有实据）：

- **project-codex**（1 记忆 + 6 trace，10:32–11:02）：codex 插件旧版（v0.3.0 自动探测
  链上线前）jsonc 写死 project_tag=_project-codex（rollout 10:33 status 输出实锤
  项目标签: ..._project-codex）。当时 codex CLI 会话实际 cwd 是 memory_recall，所以
  这是 tag 错配遗留池（即插件 notes 里"空池看不到既有项目记忆"的根因）；E2E 测试记忆
  "插件端到端测试已打通"落在其中。
- **project-.codex**（1 记忆 + 13 trace，12:41–13:45）：opencode 插件在
  /home/wbaifan/.codex 目录下开的会话（opencode.db session 12:41/12:45 directory
  实锤，topic=VS Code Codex 扩展 bug + opencodex 配置排查）。opencode 的
  getProjectTag 用 path.basename(directory) 且**不过滤点号目录**，故生成
  project-.codex；VS Code 扩展修复记忆（13:45）落在此池。
- 差异点：codex 插件自动探测链明确拒绝 "." 开头目录名，opencode 插件没有这层过滤
  ——两个插件对隐藏目录的处理不一致（潜在改进点，未改）。

结论：两者都是今天插件改造期间的产物，非数据损坏；.codex 容器后续在 ~/.codex
开会话还会被复用（合理），project-codex 为废弃错配池（仅 1 条测试记忆）。

### 处置（用户确认后已执行，14:00–14:10）

1. project-.codex 的 VS Code 扩展修复记忆 → 转存 user 范围（mem_5138c51e，error-solution），
   原 mem_4859448e 软删除（可 restore）；
2. project-codex 的 E2E 测试记忆 mem_a2cc0f 软删除（无独立价值）；
3. 修复 opencode 插件 getProjectTag 不过滤点号目录的缺陷：隐藏目录跳过自动生成，
   回退 git 仓库根目录名，再无则 project-default（与 codex 插件探测链一致），
   新增 test/project-tag.test.ts 7 用例全绿，bun run build 已产出新 dist；
   已重启 opencode serve（14:14，PID 718405）生效；后端与 codex 插件无改动无需重启。
4. 全量插件测试 144 pass / 2 fail，两条失败（injection-strategy、context-integration）
   为改动前已存在的预存问题，与本次无关。

## 记忆维护闭环实施（ADR-0009，用户授权自主决策）

背景：用户问"agent 是否能主动更新记忆"，随后授权"仔细研究思考后决定"。
调研结论：工具层（add/update/forget/restore）与写侧自动闭环（relation_service 矛盾降级）
已存在；缺口在**读侧无陈旧度信号** + 规则无触发条件（2026-08-13 出现"同会话记忆 1 小时
后过期未被发现"的实例）。上位 issue：MR-011（P0，知识浏览/纠错闭环）。

决策（ADR-0009）：**规则约束 + 注入可见性；不做定时扫描、不做自动写库**。
理由：旧≠过期，年龄扫描信噪比低；自动写库有 LLM 误判覆盖用户知识风险，与 ADR-0006
信任原则一致；矛盾信号只在内容被使用时出现，读侧标注恰好把维护责任落到那一刻。

实施内容：

1. **注入可见性（后端）**：context-inject 输出中对超过 90 天（STALE_DAYS）的记忆追加
   「记录于 N 天前」；DedupItem 增加 created_at 字段，语义搜索/记忆图谱/实体图谱三条
   路径补齐时间透传；测试 test_stale_annotation.py 6 用例（中英文、无/坏时间、关系类型）。
   实测：auto_publish 记忆（107 天）在真实注入中显示「记录于 107 天前」。
2. **规则层**：AGENTS.md「记忆维护检查点」（结论/行为变更后收尾前 search+update）；
   codex 插件 SKILL.md「记忆维护」节加主动维护条款（仓库 + 已安装缓存两处同步）。
3. **文档**：ADR-0009（Accepted）+ decisions 索引 + MR-011 进展注记 + STATUS 更新。

验证：379 passed / 14 skipped 单元回归；API 容器已重启生效（06:26 UTC）。
