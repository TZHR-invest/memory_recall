# MR-003: 文档无版本/变更历史，更新即销毁旧知识

> 状态: OPEN · 严重度: P1 · 创建: 2026-08-12

## 问题

文档更新是位置化 chunk 替换（`document_store.update_chunks`），旧 chunk 直接删除/覆盖；
对比 memories 有版本链，文档的知识演化是丢历史的。插件追踪器只知道 hash 变了，
不知道"变了什么"。

## 建议

documents 增加版本/变更记录（或复用 chunk 级变更日志），至少保留"上一版内容"可回看。

## 解决记录

（修复后填写 commit / 版本）
