# MR-004: 异步队列纯内存，进程退出丢任务

> 状态: OPEN · 严重度: P2 · 创建: 2026-08-12

## 问题

opencode 插件的 asyncQueue 是内存队列（插件 README 自述 v1 限制），进程退出时
pending/running 任务丢失；后端文档 `status` 状态机存在，但两者没有衔接。

## 建议

队列持久化（本地文件/后端任务表），或明确文档异步处理的唯一所有者（后端）。

## 解决记录

（修复后填写 commit / 版本）
